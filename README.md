# vulnerable-web-project
Cyberjutsut Summer Trainning

## Run with docker-compose
Start a container:
``docker-compose up -d --build ``

Stop a container:
``docker-compose down ``

## Run with bash script
``bash run.sh ``
